{% extends '@nucleus/partials/particle.html.twig' %}

{% block particle %}
	<h1 class="page-heading">
    {% if particle.title %}
      {{particle.title}}
    {% else %}
      {{page.title}}
    {% endif %}
	</h1>
{% endblock %}
